namespace did {

};

[External="didtoolbox"]
typedef extern DidDoc;

[Error]
enum DidResolveError {
  "DidNotSupported",
  "HttpError"
};

interface Did {
    constructor(string text);
    [Throws=DidResolveError]
    DidDoc resolve();
};